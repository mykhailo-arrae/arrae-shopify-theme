{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/sections/section-70-30/section.liquid}] %}

<style>
.tvg_section_70_30_zpnDjq_section{z-index:1;color:oklch(34.85% 0 0);background-color:oklch(99.59% .0093 99.98);width:100%;position:relative}
.tvg_section_70_30_zpnDjq_root{width:100%;max-width:94.5rem;margin-inline:auto;padding:1.25rem 1rem}
.tvg_section_70_30_zpnDjq_container{flex-direction:column;gap:2rem;width:100%;display:flex}
.tvg_section_70_30_zpnDjq_media-column,.tvg_section_70_30_zpnDjq_content-column{width:100%}
.tvg_section_70_30_zpnDjq_media{aspect-ratio:4/5;background-color:oklch(94.31% 0 0);border-radius:.5rem;position:relative;overflow:hidden}
.tvg_section_70_30_zpnDjq_media__image{width:100%;height:100%;position:absolute;inset:0}
.tvg_section_70_30_zpnDjq_media__image--placeholder{object-fit:cover}
.tvg_section_70_30_zpnDjq_media__video{z-index:1;width:100%;height:100%;position:absolute;inset:0}
.tvg_section_70_30_zpnDjq_media__video :is(video,img){object-fit:cover;width:100%;height:100%}
.tvg_section_70_30_zpnDjq_media__embed{z-index:1;border:0;width:100%;height:100%;position:absolute;inset:0}
.tvg_section_70_30_zpnDjq_media__controls{z-index:2;pointer-events:none;gap:1rem;display:flex;position:absolute;bottom:1rem;left:1rem}
.tvg_section_70_30_zpnDjq_media__controls>*{pointer-events:auto}
.tvg_section_70_30_zpnDjq_content{text-align:left;flex-direction:column;align-items:flex-start;gap:1.5rem;width:100%;padding-inline:.5rem;display:flex}
.tvg_section_70_30_zpnDjq_content__copy{overflow-wrap:anywhere;flex-direction:column;align-items:flex-start;gap:1rem;width:100%;display:flex}
.tvg_section_70_30_zpnDjq_headline{letter-spacing:-.045rem;color:oklch(21.71% .0232 69.07);letter-spacing:-.03em;text-wrap:balance;margin:0;font-family:Cardinal Fruit,Georgia,Book Antiqua,Times New Roman,serif;font-size:2rem;font-weight:400;line-height:1.12}
.tvg_section_70_30_zpnDjq_body{letter-spacing:-.01em;color:oklch(21.71% .0232 69.07);margin:0;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:1rem;font-weight:300;line-height:120%}
.tvg_section_70_30_zpnDjq_body :is(p,ul,ol){margin:0}
.tvg_section_70_30_zpnDjq_body :is(p,ul,ol)+:is(p,ul,ol){margin-top:1.125rem}
.tvg_section_70_30_zpnDjq_body a{color:inherit;text-underline-offset:.25rem;text-decoration:underline}
.tvg_section_70_30_zpnDjq_body a:hover{opacity:.7}
.tvg_section_70_30_zpnDjq_signature{aspect-ratio:180/64;mix-blend-mode:multiply;width:100%;max-width:11.25rem;position:relative}
.tvg_section_70_30_zpnDjq_signature :is(video,img){object-fit:contain;width:100%;height:100%;position:absolute;inset:0}
.tvg_section_70_30_zpnDjq_cta{margin:0}
@media print,screen and (width>=48em){.tvg_section_70_30_zpnDjq_media{aspect-ratio:3/2}
}
@media print,screen and (width>=64em){.tvg_section_70_30_zpnDjq_root{padding:3rem}
.tvg_section_70_30_zpnDjq_container{grid-template-columns:6.2fr 3fr;gap:1.5rem;display:grid}
[data-reverse=true] .tvg_section_70_30_zpnDjq_container{grid-template-columns:3fr 6.2fr}
.tvg_section_70_30_zpnDjq_media-column{grid-column:1}
[data-reverse=true] .tvg_section_70_30_zpnDjq_media-column,.tvg_section_70_30_zpnDjq_content-column{grid-column:2}
[data-reverse=true] .tvg_section_70_30_zpnDjq_content-column{grid-area:1/1}
.tvg_section_70_30_zpnDjq_media{aspect-ratio:5/3}
.tvg_section_70_30_zpnDjq_content{padding-inline:0}
.tvg_section_70_30_zpnDjq_headline{letter-spacing:-.105rem;font-family:Cardinal Fruit,Georgia,Book Antiqua,Times New Roman,serif;font-size:3.5rem;font-weight:400;line-height:98%}
.tvg_section_70_30_zpnDjq_body{font-size:1.125rem}
.tvg_section_70_30_zpnDjq_body :is(p,ul,ol)+:is(p,ul,ol){margin-top:3rem}
.tvg_section_70_30_zpnDjq_signature{aspect-ratio:277/98;max-width:17.3125rem}
}
</style>

{% liquid
  assign headline = section.settings.headline | default: ''
  assign body = section.settings.body | default: ''

  assign cta_label = section.settings.cta_label
  assign cta_url = section.settings.cta_url
  assign cta_open_in_new_tab = section.settings.cta_open_in_new_tab
  assign cta_target = '_self'
  if cta_open_in_new_tab
    assign cta_target = '_blank'
  endif

  assign reverse_columns = section.settings.reverse_columns

  assign background_type = section.settings.background_type | default: 'image'
  assign desktop_image = section.settings.desktop_image
  assign desktop_video = section.settings.desktop_video
  assign mobile_image = section.settings.mobile_image | default: desktop_image
  assign mobile_video = section.settings.mobile_video | default: desktop_video
  assign image_alt_text = headline | strip_html | strip

  assign signature_video = section.settings.signature_video

  assign show_play_button = section.settings.show_play_button
  assign show_mute_button = section.settings.show_mute_button

  assign video_class = 'js-section-70-30-video-' | append: section.id

  assign is_first_section = false
  if section.index == 1
    assign is_first_section = true
  endif

  assign image_lazyload = true
  assign image_preload = false
  assign image_fetchpriority = 'low'
  if is_first_section
    assign image_lazyload = false
    assign image_preload = true
    assign image_fetchpriority = 'high'
  endif
%}

<div
  class="tvg_section_70_30_zpnDjq_root"
  data-section-id="{{ section.id }}"
  data-reverse="{{ reverse_columns }}"
>
  <div class="tvg_section_70_30_zpnDjq_container">
    <div class="tvg_section_70_30_zpnDjq_media-column">
      <div class="tvg_section_70_30_zpnDjq_media">
        {% if background_type == 'image' %}
          {% render 'core-image',
            image: desktop_image,
            image_mobile: mobile_image,
            image_alt_text: image_alt_text,
            object_fit: 'cover',
            class: 'tvg_section_70_30_zpnDjq_media__image',
            lazyload: image_lazyload,
            preload: image_preload,
            fetchpriority: image_fetchpriority,
            srcset_print_sizes: '480,640,800,960,1280,1600',
            mobile_srcset_print_sizes: '320,480,640,768'
          %}
        {% elsif background_type == 'video' %}
          <div class="tvg_section_70_30_zpnDjq_media__video">
            {% render 'core-video',
              video: desktop_video,
              video_mobile: mobile_video,
              class: video_class,
              autoplay: false,
              loop: true,
              muted: true,
              playsinline: true,
              lazyload: true
            %}
          </div>
          <div class="tvg_section_70_30_zpnDjq_media__controls">
            {%- comment -%}
              `pause_page_videos_on_play: false` keeps the decorative signature
              video alongside this one running. Both videos in this section are
              muted (autoplay requires it), so there is no audio collision to
              guard against.
            {%- endcomment -%}
            {% render 'core-video-controls',
              video_class: video_class,
              has_pause: show_play_button,
              has_mute: show_mute_button,
              scroll_autoplay: true,
              pause_page_videos_on_play: false
            %}
          </div>
        {% elsif background_type == 'embedded_video' %}
          {%- if section.settings.video_url.type == 'youtube' -%}
            <iframe
              src="https://www.youtube.com/embed/{{ section.settings.video_url.id }}?enablejsapi=1"
              class="tvg_section_70_30_zpnDjq_media__embed js-youtube"
              title="{{ image_alt_text | default: 'Embedded video' | escape }}"
              allow="autoplay; encrypted-media; picture-in-picture"
              allowfullscreen
              loading="lazy"
            ></iframe>
          {%- else -%}
            <iframe
              src="https://player.vimeo.com/video/{{ section.settings.video_url.id }}"
              class="tvg_section_70_30_zpnDjq_media__embed js-vimeo"
              title="{{ image_alt_text | default: 'Embedded video' | escape }}"
              allow="autoplay; encrypted-media; picture-in-picture"
              allowfullscreen
              loading="lazy"
            ></iframe>
          {%- endif -%}
        {% endif %}
      </div>
    </div>

    <div class="tvg_section_70_30_zpnDjq_content-column">
      <div class="tvg_section_70_30_zpnDjq_content">
        {% if headline != blank or body != blank %}
          <div class="tvg_section_70_30_zpnDjq_content__copy">
            {% if headline != blank %}
              {% if is_first_section %}
                <h1 class="tvg_section_70_30_zpnDjq_headline">{{ headline | newline_to_br }}</h1>
              {% else %}
                <h2 class="tvg_section_70_30_zpnDjq_headline">{{ headline | newline_to_br }}</h2>
              {% endif %}
            {% endif %}

            {% if body != blank %}
              <div class="tvg_section_70_30_zpnDjq_body">{{ body }}</div>
            {% endif %}
          </div>
        {% endif %}

        {% if signature_video != blank %}
          {%- comment -%}
            Decorative animated signature — autoplay + muted + loop is enough
            for native browser autoplay; no controls needed.
          {%- endcomment -%}
          <div class="tvg_section_70_30_zpnDjq_signature" aria-hidden="true">
            {% render 'core-video',
              video: signature_video,
              autoplay: true,
              loop: true,
              loop_count: section.settings.signature_video_loop_count,
              muted: true,
              playsinline: true,
              lazyload: true,
              skip_focus: true
            %}
          </div>
        {% endif %}

        {% if cta_label != blank and cta_url != blank %}
          {% render 'core-button',
            url: cta_url,
            label: cta_label,
            target: cta_target,
            style: 'secondary',
            class: 'tvg_section_70_30_zpnDjq_cta'
          %}
        {% endif %}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Section 70/30",
  "tag": "section",
  "class": "tvg_section_70_30_zpnDjq_section",
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "checkbox",
      "id": "reverse_columns",
      "label": "Reverse columns on desktop",
      "info": "On mobile the media column is always shown first.",
      "default": false
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "textarea",
      "id": "headline",
      "label": "Headline",
      "default": "The Visionary Minds Behind Arrae's Success and Innovation"
    },
    {
      "type": "richtext",
      "id": "body",
      "label": "Body copy",
      "default": "<p>Meet our co-founders, Siff and Nish, a husband and wife duo who set out in 2020 to redefine holistic wellness, for themselves and for you.</p>"
    },
    {
      "type": "header",
      "content": "Call to action"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "default": "Our story"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Button link",
      "info": "Supports both internal pages and external URLs."
    },
    {
      "type": "checkbox",
      "id": "cta_open_in_new_tab",
      "label": "Open link in new tab",
      "default": false
    },
    {
      "type": "select",
      "id": "background_type",
      "label": "Background Type",
      "options": [
        {
          "value": "image",
          "label": "Image"
        },
        {
          "value": "video",
          "label": "Video"
        },
        {
          "value": "embedded_video",
          "label": "Embedded Video (YouTube or Vimeo)"
        }
      ],
      "default": "image"
    },
    {
      "type": "header",
      "content": "Desktop media",
      "info": "Used on screens 1024px and wider."
    },
    {
      "type": "image_picker",
      "id": "desktop_image",
      "label": "Image",
      "info": "Used when the background type is set to image.",
      "visible_if": "{{ section.settings.background_type == 'image' }}"
    },
    {
      "type": "video",
      "id": "desktop_video",
      "label": "Video",
      "info": "When set, autoplays muted and loops. Recommended aspect ratio: 5:3.",
      "visible_if": "{{ section.settings.background_type == 'video' }}"
    },
    {
      "type": "video_url",
      "id": "video_url",
      "accept": ["youtube", "vimeo"],
      "default": "https://www.youtube.com/watch?v=_9VUPq3SxOc",
      "label": "Video URL",
      "placeholder": "https://www.youtube.com/watch?v=_9VUPq3SxOc",
      "info": "Supports both YouTube and Vimeo. It is used for desktop and mobile.",
      "visible_if": "{{ section.settings.background_type == 'embedded_video' }}"
    },
    {
      "type": "header",
      "content": "Mobile media",
      "info": "Used on screens below 1024px. Falls back to the desktop image if left blank."
    },
    {
      "type": "image_picker",
      "id": "mobile_image",
      "label": "Image",
      "visible_if": "{{ section.settings.background_type == 'image' }}"
    },
    {
      "type": "video",
      "id": "mobile_video",
      "label": "Video",
      "info": "Recommended aspect ratio: 4:5.",
      "visible_if": "{{ section.settings.background_type == 'video' }}"
    },
    {
      "type": "header",
      "content": "Video controls",
      "info": "Only shown when a video is set."
    },
    {
      "type": "checkbox",
      "id": "show_play_button",
      "label": "Show play / pause button",
      "default": true,
      "visible_if": "{{ section.settings.background_type == 'video' }}"
    },
    {
      "type": "checkbox",
      "id": "show_mute_button",
      "label": "Show mute / unmute button",
      "default": true,
      "visible_if": "{{ section.settings.background_type == 'video' }}"
    },
    {
      "type": "header",
      "content": "Animated signature",
      "info": "Optional small looping video shown above the CTA button (e.g. an animated handwritten signature)."
    },
    {
      "type": "video",
      "id": "signature_video",
      "label": "Signature video"
    },
    {
      "type": "number",
      "id": "signature_video_loop_count",
      "label": "Signature video loop count",
      "info": "How many times the signature video plays before stopping on its final frame. Leave blank for continuous looping.",
      "default": 4
    }
  ],
  "presets": [
    {
      "name": "Section 70/30",
      "category": "Content"
    }
  ]
}
{% endschema %}

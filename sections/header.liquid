{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/sections/header/section.liquid}] %}

<script class="json-portable-section-assets" type="application/json">
{"sectionName":"header"}
</script>

<style>
body:has(.js-announcement-section) .tvg_header_BIfLoG_root .tvg_header_BIfLoG_spacer{height:5rem}
body:has(.js-announcement-section) .tvg_header_BIfLoG_root .tvg_header_BIfLoG_container{top:2rem}
:is(body:has(.js-announcement-section) .tvg_header_BIfLoG_root[data-scrolled=true] .tvg_header_BIfLoG_container,body:has(.js-announcement-section) .tvg_header_BIfLoG_root[data-hidden=true] .tvg_header_BIfLoG_container){top:0}
.tvg_header_BIfLoG_spacer{width:100%;height:3rem;display:block;position:relative}
.tvg_header_BIfLoG_root{z-index:2;--tvg_header_BIfLoG_megamenu-chrome-height:4rem;width:100%;transition:z-index 0s linear .5s;position:relative}
.tvg_header_BIfLoG_root[data-mobile-drawer-open=true],.tvg_header_BIfLoG_root[data-megamenu-open=true]{z-index:20;transition:z-index linear}
.tvg_header_BIfLoG_root[data-scrolled=true] .tvg_header_BIfLoG_container[data-transparent=true],.tvg_header_BIfLoG_root[data-megamenu-open=true] .tvg_header_BIfLoG_container[data-transparent=true]{color:oklch(21.71% .0232 69.07);background-color:oklch(99.59% .0093 99.98)}
.tvg_header_BIfLoG_root:not([data-scrolled=true],[data-megamenu-open=true]) .tvg_header_BIfLoG_container[data-transparent=true] .tvg_header_BIfLoG_logo__wrapper--swap .tvg_header_BIfLoG_logo__image--light{opacity:1;visibility:visible;pointer-events:auto;transition:opacity .25s,visibility linear}
.tvg_header_BIfLoG_root:not([data-scrolled=true],[data-megamenu-open=true]) .tvg_header_BIfLoG_container[data-transparent=true] .tvg_header_BIfLoG_logo__wrapper--swap .tvg_header_BIfLoG_logo__image--dark{opacity:0;visibility:hidden;pointer-events:none;transition:opacity .25s,visibility 0s linear .25s}
.tvg_header_BIfLoG_root[data-hidden=true] .tvg_header_BIfLoG_container{transform:translateY(-100%)}
body:has(.js-announcement-section) .tvg_header_BIfLoG_root:not([data-scrolled=true]){--tvg_header_BIfLoG_megamenu-chrome-height:6rem}
.tvg_header_BIfLoG_container{color:oklch(21.71% .0232 69.07);background-color:oklch(99.59% .0093 99.98);width:100%;height:3rem;transition:transform .5s cubic-bezier(.4,0,.2,1),top .5s cubic-bezier(.4,0,.2,1),background-color .25s,color .25s;position:fixed;top:0;left:0}
.tvg_header_BIfLoG_container[data-transparent=true]{color:oklch(99.59% .0093 99.98);background-color:#0000}
.tvg_header_BIfLoG_megamenu-backdrop{z-index:0;pointer-events:none;visibility:hidden;opacity:0;-webkit-backdrop-filter:blur(2.5px);backdrop-filter:blur(2.5px);background-color:oklch(21.71% .0232 69.07/.2);transition:opacity .25s,visibility 0s linear .25s;position:fixed;inset:0}
.tvg_header_BIfLoG_root[data-megamenu-open=true] .tvg_header_BIfLoG_megamenu-backdrop{visibility:visible;opacity:1;pointer-events:auto;transition:opacity .25s,visibility linear}
.tvg_header_BIfLoG_root[data-design-mode=true] .tvg_header_BIfLoG_megamenu-backdrop{display:none}
.tvg_header_BIfLoG_wrapper{justify-content:space-between;align-items:center;gap:1rem;width:100%;max-width:94.5rem;height:100%;margin-inline:auto;padding-inline:1.5rem;display:flex}
.tvg_header_BIfLoG_logo{flex:none;align-items:center;display:flex}
.tvg_header_BIfLoG_logo__link{color:currentColor;align-items:center;height:1.5rem;text-decoration:none;display:flex}
.tvg_header_BIfLoG_logo__link:focus-visible{outline-offset:.25rem}
.tvg_header_BIfLoG_logo__wrapper{align-items:center;height:100%;display:grid}
.tvg_header_BIfLoG_logo__wrapper>*{grid-area:1/1}
.tvg_header_BIfLoG_logo__wrapper--swap .tvg_header_BIfLoG_logo__image{transition:opacity .25s,visibility 0s linear .25s}
.tvg_header_BIfLoG_logo__wrapper--swap .tvg_header_BIfLoG_logo__image--light{opacity:0;visibility:hidden;pointer-events:none}
.tvg_header_BIfLoG_logo__wrapper--swap .tvg_header_BIfLoG_logo__image--dark{opacity:1;visibility:visible;pointer-events:auto;transition:opacity .25s,visibility linear}
.tvg_header_BIfLoG_logo__image{width:auto;height:100%;display:block}
.tvg_header_BIfLoG_logo__text{clip:rect(1px,1px,1px,1px);white-space:nowrap;width:1px;height:1px;overflow:hidden;position:absolute!important}
.tvg_header_BIfLoG_logo__text--fallback{letter-spacing:.06em;text-transform:uppercase;white-space:nowrap;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.75rem;font-weight:500;line-height:1.06}
.tvg_header_BIfLoG_menu{color:oklch(21.71% .0232 69.07);z-index:10000;visibility:hidden;background-color:oklch(99.59% .0093 99.98);flex-direction:column;width:100%;max-width:24.375rem;transition:transform .5s cubic-bezier(.32,.72,0,1),visibility 0s linear .5s;display:flex;position:fixed;top:0;bottom:0;right:0;overflow:hidden;transform:translate(100%)}
.tvg_header_BIfLoG_menu[data-mobile-drawer-open=true]{visibility:visible;transition:transform .5s cubic-bezier(.32,.72,0,1),visibility linear;transform:translate(0)}
.tvg_header_BIfLoG_menu__list{overscroll-behavior:contain;scrollbar-width:thin;scrollbar-color:oklch(21.71% .0232 69.07/.2) transparent;flex-direction:column;flex:auto;align-items:stretch;gap:1rem;width:100%;min-height:0;margin:0;padding:0;padding-block:0 3rem;padding-inline:1.5rem;list-style:none;display:flex;overflow:hidden auto}
.tvg_header_BIfLoG_menu__list::-webkit-scrollbar{width:.375rem}
.tvg_header_BIfLoG_menu__list::-webkit-scrollbar-track{background-color:#0000}
.tvg_header_BIfLoG_menu__list::-webkit-scrollbar-thumb{background-color:oklch(21.71% .0232 69.07/.2);border-radius:9999px}
.tvg_header_BIfLoG_menu__list::-webkit-scrollbar-thumb:hover{background-color:oklch(21.71% .0232 69.07)}
.tvg_header_BIfLoG_drawer-triggers{display:none}
.tvg_header_BIfLoG_drawer-triggers>:first-child{padding-inline-start:0}
.tvg_header_BIfLoG_mobile-drawer-header{z-index:1;background-color:oklch(99.59% .0093 99.98);flex-shrink:0;justify-content:flex-end;align-items:center;padding:.75rem 1rem;display:flex}
.tvg_header_BIfLoG_mobile-drawer-close{appearance:none;color:currentColor;cursor:pointer;background:0 0;border:none;justify-content:center;align-items:center;width:2.5rem;height:2.5rem;margin:0;padding:0;display:inline-flex}
.tvg_header_BIfLoG_mobile-drawer-close svg{width:100%;height:100%}
.tvg_header_BIfLoG_drawer-extras{flex-direction:column;align-items:flex-start;gap:1rem;list-style:none;display:flex}
.tvg_header_BIfLoG_drawer-extras__link{letter-spacing:-.0125rem;color:oklch(34.85% 0 0);font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:1.25rem;font-weight:300;line-height:1.12;text-decoration:none;display:inline-block;position:relative}
.tvg_header_BIfLoG_drawer-extras__link:before{content:"";transform-origin:0;background:currentColor;width:100%;height:1px;transition:transform .3s ease-in-out;display:block;position:absolute;bottom:0;transform:scaleX(0)}
.tvg_header_BIfLoG_drawer-extras__link:hover:before{transform-origin:0;transform:scaleX(1)}
.tvg_header_BIfLoG_drawer-extras__link:not(:hover):before{transform-origin:100%;transform:scaleX(0)}
.tvg_header_BIfLoG_drawer-extras__link:focus-visible{outline-offset:.25rem}
.tvg_header_BIfLoG_drawer-extras__markets-wrap{position:relative}
.tvg_header_BIfLoG_drawer-extras__currency{appearance:none;color:oklch(34.85% 0 0);letter-spacing:.0525rem;text-transform:uppercase;padding:0;cursor:pointer;white-space:nowrap;background:0 0;border:.03125rem solid oklch(34.85% 0 0);border-radius:.25rem;justify-content:center;align-items:center;gap:.25rem;width:5rem;height:2.375rem;margin:0;padding:.25rem .25rem .25rem .5rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.875rem;font-weight:300;line-height:1.1;transition:opacity .2s;display:inline-flex}
.tvg_header_BIfLoG_drawer-extras__currency-label{display:block}
.tvg_header_BIfLoG_drawer-extras__currency-chevron{flex-shrink:0;justify-content:center;align-items:center;width:2rem;height:2rem;display:inline-flex}
.tvg_header_BIfLoG_drawer-extras__currency-chevron svg{width:100%;height:100%}
.tvg_header_BIfLoG_mobile-drawer-bg{z-index:9999;-webkit-backdrop-filter:blur(2.5px);backdrop-filter:blur(2.5px);opacity:0;visibility:hidden;pointer-events:none;cursor:pointer;background-color:oklch(21.71% .0232 69.07/.2);transition:opacity .5s,visibility 0s linear .5s;position:fixed;inset:0}
.tvg_header_BIfLoG_root[data-mobile-drawer-open=true] .tvg_header_BIfLoG_mobile-drawer-bg{opacity:1;visibility:visible;pointer-events:auto;transition:opacity .5s,visibility linear}
:is(body.is-resizing .tvg_header_BIfLoG_menu,body.is-resizing .tvg_header_BIfLoG_mobile-drawer-bg){transition:none!important}
.tvg_header_BIfLoG_wrapper{overflow:visible}
.tvg_header_BIfLoG_actions{flex:none;justify-content:flex-end;align-items:center;gap:.5rem;display:flex;position:relative}
.tvg_header_BIfLoG_actions__icon{appearance:none;color:currentColor;cursor:pointer;background:0 0;border:none;justify-content:center;align-items:center;width:1.75rem;height:1.75rem;margin:0;padding:0;transition:opacity .25s;display:flex}
.tvg_header_BIfLoG_actions__icon:hover,.tvg_header_BIfLoG_actions__icon:focus-visible{opacity:.6}
.tvg_header_BIfLoG_actions__icon svg{width:100%;height:100%}
.tvg_header_BIfLoG_actions__icon--account{display:none}
.tvg_header_BIfLoG_actions__icon--search{width:1.5rem;height:1.5rem}
.tvg_header_BIfLoG_markets-wrap{display:contents;position:relative}
.tvg_header_BIfLoG_markets{appearance:none;color:currentColor;letter-spacing:.04em;text-transform:uppercase;padding:0;cursor:pointer;white-space:nowrap;background:0 0;border:.03125rem solid;border-radius:.25rem;align-items:center;gap:.25rem;height:1.375rem;margin:0;padding:.25rem .25rem .25rem .5rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.75rem;font-weight:500;line-height:1.06;transition:opacity .25s;display:none}
.tvg_header_BIfLoG_markets:hover,.tvg_header_BIfLoG_markets:focus-visible{opacity:.7}
.tvg_header_BIfLoG_markets__chevron{justify-content:center;align-items:center;width:1.5rem;height:1.5rem;display:flex}
.tvg_header_BIfLoG_markets__chevron svg{width:100%;height:100%}
.tvg_header_BIfLoG_cart{position:relative}
.tvg_header_BIfLoG_cart__count{color:oklch(21.71% .0232 69.07);letter-spacing:.04em;background-color:oklch(99.59% .0093 99.98);border-radius:9999px;justify-content:center;align-items:center;min-width:1rem;height:1rem;padding:0 .25rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.625rem;font-weight:500;line-height:1;display:flex;position:absolute;top:-.25rem;right:-.25rem}
.tvg_header_BIfLoG_container:not([data-transparent=true]) .tvg_header_BIfLoG_cart__count,.tvg_header_BIfLoG_root[data-scrolled=true] .tvg_header_BIfLoG_container[data-transparent=true] .tvg_header_BIfLoG_cart__count,.tvg_header_BIfLoG_root[data-megamenu-open=true] .tvg_header_BIfLoG_container[data-transparent=true] .tvg_header_BIfLoG_cart__count{color:oklch(99.59% .0093 99.98);background-color:oklch(21.71% .0232 69.07)}
.tvg_header_BIfLoG_cart__count[data-item-count="0"]{display:none}
.tvg_header_BIfLoG_cart__sr{clip:rect(1px,1px,1px,1px);white-space:nowrap;width:1px;height:1px;overflow:hidden;position:absolute!important}
.tvg_header_BIfLoG_hamburger{appearance:none;color:currentColor;cursor:pointer;background:0 0;border:none;justify-content:center;align-items:center;width:2rem;height:2rem;margin:0;padding:0;transition:opacity .25s;display:flex}
.tvg_header_BIfLoG_hamburger:hover,.tvg_header_BIfLoG_hamburger:focus-visible{opacity:.6}
.tvg_header_BIfLoG_hamburger svg{width:100%;height:100%}
@media print,screen and (width>=64em){body:has(.js-announcement-section) .tvg_header_BIfLoG_root .tvg_header_BIfLoG_spacer{height:6rem}
.tvg_header_BIfLoG_spacer,.tvg_header_BIfLoG_container{height:4rem}
.tvg_header_BIfLoG_wrapper{gap:2rem;padding-inline:3rem}
.tvg_header_BIfLoG_wrapper:has(.tvg_header_BIfLoG_menu[data-drawer-mode-on-desktop=true]) .tvg_header_BIfLoG_logo,.tvg_header_BIfLoG_wrapper:has(.tvg_header_BIfLoG_menu[data-drawer-mode-on-desktop=true]) .tvg_header_BIfLoG_actions{flex:none}
.tvg_header_BIfLoG_logo{flex:1 1 0;justify-content:flex-start}
.tvg_header_BIfLoG_logo__link{height:1.5rem}
.tvg_header_BIfLoG_menu:not([data-drawer-mode-on-desktop=true]){width:auto;max-width:none;color:inherit;z-index:auto;overscroll-behavior:auto;visibility:visible;background-color:#0000;border-radius:0;flex-direction:row;flex:1 1 0;align-self:stretch;transition:none;display:flex;position:static;overflow:visible;transform:none}
.tvg_header_BIfLoG_menu[data-drawer-mode-on-desktop=true]{left:0;right:auto;transform:translate(-100%)}
.tvg_header_BIfLoG_menu[data-drawer-mode-on-desktop=true][data-mobile-drawer-open=true]{transform:translate(0)}
.tvg_header_BIfLoG_menu:not([data-drawer-mode-on-desktop=true]) .tvg_header_BIfLoG_menu__list{overscroll-behavior:auto;flex-direction:row;flex:0 auto;justify-content:center;align-items:stretch;gap:0;width:100%;height:100%;min-height:auto;padding:0;overflow:visible}
.tvg_header_BIfLoG_drawer-triggers{flex:1 1 0;justify-content:flex-start;align-self:stretch;align-items:stretch;gap:.5rem;min-width:0;display:flex}
.tvg_header_BIfLoG_menu:not([data-drawer-mode-on-desktop=true]) .tvg_header_BIfLoG_mobile-drawer-header,.tvg_header_BIfLoG_drawer-extras,.tvg_header_BIfLoG_container:not(:has(.tvg_header_BIfLoG_menu[data-drawer-mode-on-desktop=true])) .tvg_header_BIfLoG_mobile-drawer-bg{display:none}
.tvg_header_BIfLoG_actions{flex:1 1 0;gap:1rem}
.tvg_header_BIfLoG_actions__icon--account{display:flex}
.tvg_header_BIfLoG_markets-wrap{display:block}
.tvg_header_BIfLoG_markets{display:inline-flex}
.tvg_header_BIfLoG_hamburger{display:none}
}
@media print,screen and (width<64em){.tvg_header_BIfLoG_actions__icon{width:2rem;height:2rem}
}
@media (prefers-reduced-motion:reduce){.tvg_header_BIfLoG_container,.tvg_header_BIfLoG_megamenu-backdrop,.tvg_header_BIfLoG_logo__wrapper--swap .tvg_header_BIfLoG_logo__image,.tvg_header_BIfLoG_menu,.tvg_header_BIfLoG_drawer-extras__currency,.tvg_header_BIfLoG_mobile-drawer-bg,.tvg_header_BIfLoG_actions__icon,.tvg_header_BIfLoG_markets,.tvg_header_BIfLoG_hamburger{transition:none}
}
</style>

{% liquid
  assign logo_light = section.settings.logo_light
  assign logo_dark = section.settings.logo_dark
  assign has_logo = false
  if logo_light != blank or logo_dark != blank
    assign has_logo = true
  endif
  assign swap_logos = false
  if logo_light != blank and logo_dark != blank
    assign swap_logos = true
  endif
  assign show_account_button = section.settings.show_account_button | default: null
  assign show_markets_selector = section.settings.show_markets_selector | default: null
  assign use_drawer_mode_on_desktop = section.settings.use_drawer_mode_on_desktop | default: false

  assign current_currency = localization.country.currency.iso_code | upcase
  assign current_country = localization.country

  assign transparent_header_paths = settings.transparent_header_paths | split: ','
  assign is_transparent = false
  if template == 'index' or transparent_header_paths contains request.path
    assign is_transparent = true
  endif

  assign cart_count = cart.item_count
  assign markets_label = 'sections.header.markets' | t: currency: current_currency

  assign show_drawer_markets = false
  if show_markets_selector and current_country
    assign show_drawer_markets = true
  endif

  assign show_drawer_account = false
  if shop.customer_accounts_enabled
    assign show_drawer_account = true
  endif

  assign show_drawer_extras = false
  if show_drawer_account or show_drawer_markets
    assign show_drawer_extras = true
  endif
%}

{% render 'nav-banner-card-stylesheet' %}

{% unless is_transparent %}
  <div class="tvg_header_BIfLoG_spacer" aria-hidden="true"></div>
{% endunless %}

{% comment %}
  Backdrop renders behind any open mega menu panel, blurring the page below
  while keeping the header bar and panel itself crisp.
{% endcomment %}
<div class="tvg_header_BIfLoG_megamenu-backdrop js-megamenu-backdrop" aria-hidden="true"></div>

<div
  class="tvg_header_BIfLoG_container"
  data-transparent="{% if is_transparent %}true{% else %}false{% endif %}"
>
  {% comment %}
    Backdrop for the mobile mega menu drawer. Sits behind the drawer panel
    and absorbs taps to close. Lives INSIDE `.container` (which is the
    nearest fixed-position stacking-context ancestor of `.menu`) so the
    drawer's `z-index` competes against the backdrop in the same stacking
    context — otherwise the backdrop's `backdrop-filter` would paint over
    the drawer and blur its contents.
  {% endcomment %}
  <div
    class="tvg_header_BIfLoG_mobile-drawer-bg js-mobile-drawer-bg"
    aria-hidden="true"
  ></div>

  <div class="tvg_header_BIfLoG_wrapper">
    <div class="tvg_header_BIfLoG_logo">
      <a href="/" class="tvg_header_BIfLoG_logo__link" aria-label="{{ 'sections.header.home' | t }}">
        {% if has_logo %}
          <span class="tvg_header_BIfLoG_logo__wrapper {% if swap_logos %}tvg_header_BIfLoG_logo__wrapper--swap{% endif %}">
            {% if logo_light != blank %}
              {{
                logo_light
                | image_url: width: 320
                | image_tag:
                  loading: 'eager',
                  fetchpriority: 'high',
                  class: 'tvg_header_BIfLoG_logo__image tvg_header_BIfLoG_logo__image--light',
                  widths: '120, 160, 240, 320, 480',
                  sizes: '(min-width: 1024px) 104px, 80px',
                  alt: ''
              }}
            {% endif %}
            {% if logo_dark != blank %}
              {{
                logo_dark
                | image_url: width: 320
                | image_tag:
                  loading: 'eager',
                  fetchpriority: 'high',
                  class: 'tvg_header_BIfLoG_logo__image tvg_header_BIfLoG_logo__image--dark',
                  widths: '120, 160, 240, 320, 480',
                  sizes: '(min-width: 1024px) 104px, 80px',
                  alt: ''
              }}
            {% endif %}
          </span>
          <span class="tvg_header_BIfLoG_logo__text">{{ shop.name }}</span>
        {% else %}
          <span class="tvg_header_BIfLoG_logo__text tvg_header_BIfLoG_logo__text--fallback">{{ shop.name }}</span>
        {% endif %}
      </a>
    </div>

    {% comment %}
      Inline trigger strip rendered next to the logo in drawer-mode-on-desktop
    {% endcomment %}
    {% if use_drawer_mode_on_desktop %}
      <div class="tvg_header_BIfLoG_drawer-triggers js-drawer-triggers"></div>
    {% endif %}

    {% if section.blocks.size > 0 %}
      <nav
        class="tvg_header_BIfLoG_menu js-header-nav js-mobile-drawer"
        aria-label="{{ 'sections.header.primary_menu' | t }}"
        data-mobile-drawer-open="false"
        data-drawer-mode-on-desktop="{% if use_drawer_mode_on_desktop %}true{% else %}false{% endif %}"
      >
        <div class="tvg_header_BIfLoG_mobile-drawer-header">
          <button
            type="button"
            class="tvg_header_BIfLoG_mobile-drawer-close js-mobile-drawer-close"
            aria-label="{{ 'sections.header.close_menu' | t }}"
          >
            {% render 'icon-close', width: 32, height: 32 %}
          </button>
        </div>

        <ul
          class="tvg_header_BIfLoG_menu__list"
          role="list"
          data-mobile-accordion-group="true"
        >
          {% content_for 'blocks' %}

          {% comment %}
            Account link and currency selector at the bottom of the
            scrollable drawer list. Hidden when the nav is inline on desktop.
          {% endcomment %}
          {% if show_drawer_extras %}
            <li class="tvg_header_BIfLoG_drawer-extras">
              {% if show_drawer_account %}
                <a
                  href="{{ routes.account_url }}"
                  class="tvg_header_BIfLoG_drawer-extras__link"
                >
                  {{ 'sections.header.account' | t }}
                </a>
              {% endif %}

              {% if show_drawer_markets %}
                {% comment %}
                  Mobile wrap for the markets dropdown. Anchors the popover
                  *above* the chip via `data-markets-position='above'`.
                {% endcomment %}
                <div
                  class="tvg_header_BIfLoG_drawer-extras__markets-wrap js-markets-wrap"
                  data-markets-position="above"
                >
                  <button
                    type="button"
                    class="tvg_header_BIfLoG_drawer-extras__currency js-markets-trigger"
                    aria-haspopup="dialog"
                    aria-expanded="false"
                    aria-label="{{ markets_label }}"
                  >
                    <span class="tvg_header_BIfLoG_drawer-extras__currency-label">
                      {{ current_currency }}
                    </span>
                    <span
                      class="tvg_header_BIfLoG_drawer-extras__currency-chevron"
                      aria-hidden="true"
                    >
                      {% render 'icon-chevron-bottom', width: 16, height: 16 %}
                    </span>
                  </button>
                  {% render 'markets-drawer', allowed_countries: section.settings.markets_allowed_countries %}
                </div>
              {% endif %}
            </li>
          {% endif %}
        </ul>
      </nav>
    {% endif %}

    <div class="tvg_header_BIfLoG_actions">
      {% if show_markets_selector and current_country %}
        {% comment %}
          Desktop wrap for the markets dropdown. Anchors the popover *below*
          the trigger via `data-markets-position='below'`.
        {% endcomment %}
        <div
          class="tvg_header_BIfLoG_markets-wrap js-markets-wrap"
          data-markets-position="below"
        >
          <button
            type="button"
            class="tvg_header_BIfLoG_markets js-markets-trigger"
            aria-label="{{ markets_label }}"
            aria-haspopup="dialog"
            aria-expanded="false"
          >
            <span>{{ current_currency }}</span>
            <span class="tvg_header_BIfLoG_markets__chevron" aria-hidden="true">
              {% render 'icon-chevron-bottom', width: 18, height: 18 %}
            </span>
          </button>
          {% render 'markets-drawer', allowed_countries: section.settings.markets_allowed_countries %}
        </div>
      {% endif %}

      <button
        type="button"
        class="tvg_header_BIfLoG_actions__icon tvg_header_BIfLoG_actions__icon--search js-offcanvas-trigger"
        data-drawer-name="search"
        aria-label="{{ 'sections.header.search' | t }}"
        aria-haspopup="dialog"
      >
        {% render 'icon-search', width: 28, height: 28 %}
      </button>

      {% if shop.customer_accounts_enabled and show_account_button %}
        <a
          href="{{ routes.account_url }}"
          class="tvg_header_BIfLoG_actions__icon tvg_header_BIfLoG_actions__icon--account"
          aria-label="{{ 'sections.header.account' | t }}"
        >
          {% render 'icon-account', width: 28, height: 28 %}
        </a>
      {% endif %}

      <button
        type="button"
        class="tvg_header_BIfLoG_actions__icon tvg_header_BIfLoG_cart js-offcanvas-trigger"
        data-drawer-name="cart"
        aria-label="{{ 'sections.header.cart' | t }}"
        aria-haspopup="dialog"
      >
        {% render 'icon-shopping-bag', width: 28, height: 28 %}
        <span
          class="tvg_header_BIfLoG_cart__count js-cart-item-count"
          data-item-count="{{ cart_count }}"
          aria-hidden="true"
        >
          {{ cart_count }}
        </span>
        <span class="tvg_header_BIfLoG_cart__sr">
          {{ 'sections.header.cart_count' | t: count: cart_count }}
        </span>
      </button>

      {% comment %}
        Hamburger toggles the mobile mega menu drawer rendered above
      {% endcomment %}
      <button
        type="button"
        class="tvg_header_BIfLoG_hamburger js-mobile-drawer-trigger"
        aria-label="{{ 'sections.header.toggle_menu' | t }}"
        aria-haspopup="menu"
        aria-expanded="false"
      >
        {% render 'icon-menu', width: 28, height: 28 %}
      </button>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Header",
  "tag": "header",
  "enabled_on": {
    "groups": ["header"]
  },
  "class": "js-header tvg_header_BIfLoG_root",
  "settings": [
    {
      "type": "paragraph",
      "content": "Note: Sticky header functionality (color change on scroll) is disabled in design mode for better performance."
    },
    {
      "type": "header",
      "content": "Branding"
    },
    {
      "type": "image_picker",
      "id": "logo_light",
      "label": "Logo light",
      "info": "Shown when the header background is transparent (e.g. at the top of the homepage)."
    },
    {
      "type": "image_picker",
      "id": "logo_dark",
      "label": "Logo dark",
      "info": "Shown when the header background is solid white (e.g. after scrolling or on inner pages)."
    },
    {
      "type": "header",
      "content": "Actions"
    },
    {
      "type": "checkbox",
      "id": "show_markets_selector",
      "label": "Show Markets Selector",
      "default": true
    },
    {
      "type": "textarea",
      "id": "markets_allowed_countries",
      "label": "Countries to show in selector",
      "info": "Comma- or newline-separated ISO country codes in display order (e.g. US, EUR, GB). Use ISO codes (GB, not UK). Add EUR for one EU-flag euro option (hides other euro countries). If the shopper's current market isn't in the list, it's appended at the end so a selection is always visible. Leave blank to show all available countries. Configure markets in Settings → Markets."
    },
    {
      "type": "checkbox",
      "id": "show_account_button",
      "label": "Show Account Button",
      "default": true
    },
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "checkbox",
      "id": "use_drawer_mode_on_desktop",
      "label": "Use drawer mode on Desktop",
      "info": "When enabled, it replaces the desktop mega menu navigation with the same drawer navigation used on mobile.",
      "default": false
    }
  ],
  "blocks": [{ "type": "nav-item" }],
  "max_blocks": 8
}
{% endschema %}

{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/sections/search/section.liquid}] %}

<script class="json-portable-section-assets" type="application/json">
{"sectionName":"search"}
</script>

<style>
.tvg_search_el0bUq_root{width:100%;position:relative}
.tvg_search_el0bUq_root .hidden{clip:rect(1px,1px,1px,1px);white-space:nowrap;width:1px;height:1px;overflow:hidden;position:absolute!important}
.tvg_search_el0bUq_content{z-index:1;width:100%;max-width:94.5rem;margin-inline:auto;padding:0 1rem;position:relative;overflow:hidden}
.tvg_search_el0bUq_search-results-header{flex-direction:row;justify-content:space-between;align-items:center;gap:1rem;padding:.5rem;display:flex}
.tvg_search_el0bUq_container{flex-direction:column;gap:2.5rem;padding-top:1rem;padding-bottom:1.25rem;display:flex}
.tvg_search_el0bUq_actions{flex-shrink:0;justify-content:flex-end;align-items:center;gap:.75rem;display:flex}
.tvg_search_el0bUq_grid{grid-template-columns:repeat(2,minmax(0,1fr));gap:.875rem;display:grid}
.tvg_search_el0bUq_grid[data-layout="1-column"]{grid-template-columns:repeat(1,minmax(0,1fr))}
.tvg_search_el0bUq_item{z-index:1;width:100%;display:block}
.tvg_search_el0bUq_filters{appearance:none;color:oklch(34.85% 0 0);cursor:pointer;text-transform:uppercase;background:oklch(99.59% .0093 99.98);border:1px solid oklch(34.85% 0 0);border-radius:1.5rem;flex-direction:row;justify-content:center;align-items:center;gap:.75rem;height:2.5rem;margin:0;padding:0 2.5rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.75rem;font-weight:300;line-height:120%;transition:color .3s ease-in-out,background-color .3s ease-in-out,border-color .3s ease-in-out;display:flex}
.tvg_search_el0bUq_filters__qty{color:oklch(21.71% .0232 69.07);background-color:oklch(99.59% .0093 99.98);border-radius:100%;justify-content:center;align-items:center;min-width:1.25rem;height:1.25rem;padding:0 .5rem;font-size:.75rem;transition:color .3s ease-in-out,background-color .3s ease-in-out;display:flex}
.tvg_search_el0bUq_results{color:oklch(53.82% 0 0);font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.875rem;font-weight:300;line-height:120%}
.tvg_search_el0bUq_results b{font-weight:500}
.tvg_search_el0bUq_empty{text-align:center;padding-top:1.5rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:1rem;font-weight:300;line-height:120%}
.tvg_search_el0bUq_empty a{color:currentColor;text-decoration:none;display:inline-block;position:relative}
.tvg_search_el0bUq_empty a:before{content:"";transform-origin:0;background:currentColor;width:100%;height:1px;transition:transform .3s ease-in-out;display:block;position:absolute;bottom:0;left:0;transform:scaleX(1)}
.tvg_search_el0bUq_empty a:hover:before{transform-origin:0;transform:scaleX(0)}
.tvg_search_el0bUq_empty a:not(:hover):before{transform-origin:100%;transform:scaleX(1)}
.tvg_search_el0bUq_recommendations{flex-direction:column;display:flex}
.tvg_search_el0bUq_recommendations__title{letter-spacing:-.15rem;letter-spacing:-.045rem;color:oklch(34.85% 0 0);width:100%;padding:.5rem 0;font-family:Cardinal Fruit,Georgia,Book Antiqua,Times New Roman,serif;font-size:1.5rem;font-weight:400;line-height:120%}
.tvg_search_el0bUq_recommendations__container{width:100%;padding-top:1rem;padding-bottom:1.25rem;position:relative}
.tvg_search_el0bUq_recommendations__wrapper{grid-template-columns:repeat(2,minmax(0,1fr));gap:.875rem;display:grid}
.tvg_search_el0bUq_recommendations__wrapper[data-mobile-layout="1-column"]{grid-template-columns:repeat(1,minmax(0,1fr))}
.tvg_search_el0bUq_recommendations__navigation{width:100%;padding:1.5rem 1rem 0}
@media print,screen and (width>=64em){.tvg_search_el0bUq_content{padding:0 3rem}
.tvg_search_el0bUq_search-results-header{padding:.5rem 0}
.tvg_search_el0bUq_container{padding-top:1.5rem;padding-bottom:2rem}
.tvg_search_el0bUq_grid{grid-template-columns:repeat(3,minmax(0,1fr));gap:1.5rem .75rem}
.tvg_search_el0bUq_grid[data-layout="1-column"]{grid-template-columns:repeat(4,minmax(0,1fr))}
.tvg_search_el0bUq_empty{font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:1.25rem;font-weight:300;line-height:120%}
.tvg_search_el0bUq_recommendations__container{padding-top:2rem;padding-bottom:2.5rem}
.tvg_search_el0bUq_recommendations__wrapper{grid-template-columns:repeat(3,minmax(0,1fr));gap:1.5rem .75rem}
.tvg_search_el0bUq_recommendations__wrapper[data-mobile-layout="1-column"]{grid-template-columns:repeat(4,minmax(0,1fr))}
.tvg_search_el0bUq_recommendations__navigation{padding:2.5rem 0 0}
}
@media (hover:hover){.tvg_search_el0bUq_filters:hover{color:oklch(99.59% .0093 99.98);background-color:oklch(34.85% 0 0);border-color:oklch(34.85% 0 0)}
.tvg_search_el0bUq_filters:hover .tvg_search_el0bUq_filters__qty{color:oklch(99.59% .0093 99.98);background-color:oklch(21.71% .0232 69.07)}
}
</style>

{% liquid
  assign show_breadcrumbs = section.settings.show_breadcrumbs
  assign per_page = section.settings.products_per_view
  assign show_filters = section.settings.show_filters

  assign recommendations = section.blocks | where: 'type', 'recommended_product'
  assign result_title = section.settings.result_title
  assign results_title = section.settings.results_title

  assign empty_results_text = section.settings.empty_results
  assign recommendations_title = section.settings.recommendations_title

  render 'product-card-stylesheet'
  render 'product-review-ratings-stylesheet'
%}

{% capture recommendations_content %}
  {% if recommendations.size > 0 %}
    <div class='tvg_search_el0bUq_recommendations'>
      {% if recommendations_title != blank %}
        <h2 class='tvg_search_el0bUq_recommendations__title'>
          {{ recommendations_title }}
        </h2>
      {% endif %}
      <div class="tvg_search_el0bUq_recommendations__container">
        <div class='tvg_search_el0bUq_recommendations__wrapper' data-mobile-layout="2-column">
          {% for recommendation in recommendations %}
            {% render 'product-card', product: recommendation.settings.product %}
          {% endfor %}
        </div>
      </div>
    </div>
  {% endif %}
{% endcapture %}

<div class="tvg_search_el0bUq_root">
  {% if show_breadcrumbs %}
    <div class="">
      {% render 'breadcrumbs' %}
    </div>
  {% endif %}
  <div class="tvg_search_el0bUq_content">
    {%- if search.performed -%}
      <div class="tvg_search_el0bUq_search-results-header">
        <h1 class="tvg_search_el0bUq_results">
          {%- if search.results_count == 0 -%}
            {{ empty_results_text }}
            <b>"{{ search.terms }}"</b>
          {%- elsif search.results_count == 1 -%}
            {{ result_title }}
            <b>"{{ search.terms }}"</b>
          {%- else -%}
            {{ results_title | replace: '[x]', search.results_count }}
            <b>"{{ search.terms }}"</b>
          {%- endif -%}
        </h1>

        {%- if search.results_count != 0 -%}
          <div class="">
            <div class="tvg_search_el0bUq_actions js-actions">
              {% if show_filters %}
                <button
                  class="tvg_search_el0bUq_filters js-offcanvas-trigger"
                  data-drawer-name="filters"
                  data-tvg-track="button_click"
                  data-tvg-track-label="{{ 'collections.filter.button' | t }}"
                >
                  <span class="">
                    {%- render 'icon-filters' -%}
                  </span>
                  {{ 'collections.filter.button' | t }}
                </button>
              {% endif %}
            </div>
          </div>
        {% endif %}
      </div>

      {%- if search.results_count != 0 -%}
        <div
          class="tvg_search_el0bUq_container js-search-results-container"
          data-search-terms="{{ search.terms | escape }}"
          {% unless request.parameters.type == 'product' %}
            data-require-search-type-product="true"
          {% endunless %}
        >
          {% paginate search.results by per_page %}
            <div class="Grid tvg_search_el0bUq_grid js-search-grid" data-mobile-layout="2-column">
              {%- for item in search.results -%}
                <div class="tvg_search_el0bUq_item">
                  {% render 'product-card', product: item %}
                </div>
              {%- endfor -%}
            </div>

            {%- render 'pagination-load-more', paginate: paginate, items_on_page: search.results.size -%}
          {%- endpaginate -%}
        </div>

      {%- else -%}
        {{ recommendations_content }}
      {%- endif -%}
    {%- else -%}
      {{ recommendations_content }}
    {%- endif -%}
  </div>

  {%- render 'collection-filters', filters: search.filters -%}
</div>

{% schema %}
{
  "name": "Search",
  "class": "js-search-section",
  "enabled_on": {
    "templates": ["search"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "empty_results",
      "label": "Empty Results Content",
      "default": "We could not find anything for"
    },
    {
      "type": "text",
      "id": "results_title",
      "label": "Multiple results title",
      "default": "Showing [x] results for"
    },
    {
      "type": "text",
      "id": "result_title",
      "label": "1 Result Title",
      "default": "Showing 1 result for"
    },
    {
      "type": "header",
      "content": "Grid Settings"
    },
    {
      "type": "range",
      "id": "products_per_view",
      "label": "Products per view",
      "min": 2,
      "max": 36,
      "step": 2,
      "default": 18
    },
    {
      "type": "checkbox",
      "id": "show_breadcrumbs",
      "label": "Show breadcrumbs",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_filters",
      "label": "Show filters & sort",
      "default": false
    },
    {
      "type": "header",
      "content": "Recommendations (No results view)"
    },
    {
      "type": "inline_richtext",
      "id": "recommendations_title",
      "label": "Recommendations Title",
      "default": "These popular items might interest you"
    }
  ],
  "blocks": [
    {
      "type": "recommended_product",
      "name": "Recommended Product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        }
      ],
      "limit": 3
    }
  ],
  "presets": [
    {
      "name": "Search",
      "settings": {}
    }
  ]
}
{% endschema %}

{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/snippets/blog-load-more/snippet.liquid}] %}

<style>
.tvg_blog_load_more_AlzRva_root{text-align:center;flex-direction:column;justify-content:center;align-items:center;gap:.75rem;width:100%;padding:1.5rem 1rem 0;display:flex}
.tvg_blog_load_more_AlzRva_root[hidden]{display:none}
.tvg_blog_load_more_AlzRva_legend{color:oklch(34.85% 0 0);text-align:center;margin:0;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-weight:300;line-height:120%}
.tvg_blog_load_more_AlzRva_legendCount{letter-spacing:-.01rem;font-size:1rem;font-weight:500;line-height:130%}
.tvg_blog_load_more_AlzRva_legendText{letter-spacing:-.01rem;font-size:1rem;font-weight:400;line-height:120%}
.js-blog-load-more{min-width:12.5rem}
.js-blog-load-more[aria-busy=true]{cursor:progress}
.tvg_blog_load_more_AlzRva_error{color:oklch(47.54% .17 26.63);text-align:center;margin:0;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.875rem;font-weight:300;line-height:120%}
.tvg_blog_load_more_AlzRva_error[hidden]{display:none}
@media print,screen and (width>=64em){.tvg_blog_load_more_AlzRva_root{padding:2.5rem 1rem 0}
}
</style>

{% doc %}
  Async "Load more" control for the blog article grid: progress count and a
  button that appends the next page via the Section Rendering API.

  @param {paginate} paginate - Liquid paginate object (required)
  @param {section} section - Parent section (required)
  @param {number} loaded_count - Articles currently shown in the grid (required)
  @param {number} total_count - Total visible articles after exclusions (required)
  @param {number} [native_pages] - Native paginate page count (defaults to paginate.pages)
  @param {number} [native_page] - Current native paginate page (defaults to paginate.current_page)
  @param {number} [per_page] - Articles revealed per "Load more" click (default: 6)

  @example
  {% render 'blog-load-more',
    paginate: paginate,
    section: section,
    loaded_count: loaded_count,
    total_count: total_count,
    native_pages: native_pages,
    native_page: native_page,
    per_page: per_page
  %}
{% enddoc %}

{% liquid
  assign load_more_label = section.settings.load_more_label | default: 'Load more'
  assign per_page = per_page | default: 6

  if native_pages != blank
    assign resolved_native_pages = native_pages
  else
    assign resolved_native_pages = paginate.pages
  endif

  if native_page != blank
    assign resolved_native_page = native_page
  else
    assign resolved_native_page = paginate.current_page
  endif
%}

<div
  class="tvg_blog_load_more_AlzRva_root js-blog-load-more-root"
  data-total="{{ total_count }}"
  data-per-page="{{ per_page }}"
  data-native-page="{{ resolved_native_page }}"
  data-native-pages="{{ resolved_native_pages }}"
  {% if total_count <= loaded_count %}
    hidden
  {% endif %}
>
  <p
    class="tvg_blog_load_more_AlzRva_legend js-blog-load-more-count"
    role="status"
    aria-live="polite"
  >
    <strong class="tvg_blog_load_more_AlzRva_legendCount js-blog-load-more-count-value">{{ loaded_count }}</strong>
    <span class="tvg_blog_load_more_AlzRva_legendText"> out of </span>
    <strong class="tvg_blog_load_more_AlzRva_legendCount js-blog-load-more-total-value">{{ total_count }}</strong>
    <span class="tvg_blog_load_more_AlzRva_legendText"> articles</span>
  </p>

  {% render 'core-button',
    label: load_more_label,
    style: 'secondary',
    type: 'button',
    class: ' js-blog-load-more',
    attributes: 'aria-busy="false"'
  %}

  <p class="tvg_blog_load_more_AlzRva_error js-blog-load-more-error" role="alert" hidden></p>
</div>

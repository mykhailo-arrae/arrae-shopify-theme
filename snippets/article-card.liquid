{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/snippets/article-card/snippet.liquid}] %}

{% comment %}
  Example(s):
  {% render 'article-card',
    article: article
  %}

  Parameters:
  - article {Article} - The article object
{% endcomment %}

{% liquid
  assign article = article | default: null
  assign title_split = article.title | split: '|'
  assign title_first = title_split[0]
  assign title_second = title_split[1] | default: null

  assign badge = article.tags | first | default: null
%}

<div class="tvg_article_card_i1ZS1a_card js-article-card">
  <div class="tvg_article_card_i1ZS1a_card__media">
    <a class="tvg_article_card_i1ZS1a_card__media__initial" href="{{ article.url }}">
      {% if article.image != blank %}
        {% render 'core-image',
          image: article.image,
          mode: 'cover',
          object_fit: 'cover',
          lazyload: true,
          srcset_print_sizes: '320,480,640'
        %}
      {% else %}
        <div class="tvg_article_card_i1ZS1a_card__media__placeholder"></div>
      {% endif %}
    </a>
  </div>

  <div class="tvg_article_card_i1ZS1a_card__content">
    {% if article.title != blank %}
      <a class="tvg_article_card_i1ZS1a_card__title" href="{{ article.url }}">
        <span class="tvg_article_card_i1ZS1a_card__title__first">{{ title_first }}</span>
        {% if title_second != blank %}
          <span class="tvg_article_card_i1ZS1a_card__title__second">{{ title_second }}</span>
        {% endif %}
      </a>
    {% endif %}

    <div class="tvg_article_card_i1ZS1a_card__footer">
      <div class="tvg_article_card_i1ZS1a_card__byline">
        {% if article.author != blank %}
          <span class="tvg_article_card_i1ZS1a_card__author">
            {{ article.author }}
          </span>
          <span class="tvg_article_card_i1ZS1a_card__date">
            {{ article.published_at | date: format: 'month_day_year' }}
          </span>
        {% endif %}
      </div>
      {% if badge != blank %}
        <div class="tvg_article_card_i1ZS1a_card__badge">
          <span class="tvg_article_card_i1ZS1a_card__badge--label"> {{ badge }}</span>
        </div>
      {% endif %}
    </div>
  </div>
</div>

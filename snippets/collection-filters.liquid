{% # DO NOT EDIT: This file is auto-generated and will be overwritten. [build-fingerprint:assets:source={_js/snippets/collection-filters/snippet.liquid}] %}

<div class="portable-snippet" data-snippet-name="collection-filters">

<style>
.tvg_collection_filters_sXMK-G_overlay{z-index:999;cursor:pointer;visibility:hidden;-webkit-backdrop-filter:blur(2.5px);backdrop-filter:blur(2.5px);opacity:0;background-color:oklch(21.71% .0232 69.07/.2);width:100%;height:100vh;transition:visibility ease-in-out,opacity .5s ease-in-out;position:fixed;top:0;left:0}
.has-open-offcanvas[data-offcanvas=filters]{overflow:hidden!important}
.has-open-offcanvas[data-offcanvas=filters] .tvg_collection_filters_sXMK-G_overlay{visibility:visible;opacity:1}
.tvg_collection_filters_sXMK-G_root{z-index:9999;opacity:0;background-color:oklch(99.59% .0093 99.98);width:100%;height:100dvh;margin:0;padding:0;transition:transform .3s ease-in-out,opacity 4s ease-in-out;position:fixed;top:0;right:0;transform:translate(100%)translateZ(0)}
.tvg_collection_filters_sXMK-G_root.is-expanded{opacity:1;transition:transform .3s ease-in-out,opacity ease-in-out;transform:translate(0%)translateY(0%)translateZ(0)}
.tvg_collection_filters_sXMK-G_container{flex-direction:column;width:100%;height:100%;display:flex}
.tvg_collection_filters_sXMK-G_header{flex-shrink:0;justify-content:space-between;align-items:flex-start;gap:1.5rem;padding:2.5rem 1rem 0;display:flex}
.tvg_collection_filters_sXMK-G_title{color:oklch(34.85% 0 0);letter-spacing:-.045rem;width:100%;font-family:Cardinal Fruit,Georgia,Book Antiqua,Times New Roman,serif;font-size:2rem;font-weight:400;line-height:98%}
.tvg_collection_filters_sXMK-G_close{appearance:none;color:currentColor;cursor:pointer;background:0 0;border:none;flex-shrink:0;width:2.75rem;height:2.75rem;margin:0;padding:0}
.tvg_collection_filters_sXMK-G_close svg{width:100%;height:100%}
.tvg_collection_filters_sXMK-G_form{scrollbar-width:thin;scrollbar-color:oklch(73.24% .0049 78.29) oklch(99.59% .0093 99.98);flex-direction:column;flex:auto;gap:2.5rem;padding:2.5rem 1rem;display:flex;overflow-y:auto}
.tvg_collection_filters_sXMK-G_group{border:0;flex-direction:column;gap:1rem;min-inline-size:0;margin:0;padding:0;display:flex}
.tvg_collection_filters_sXMK-G_group__title{color:oklch(34.85% 0 0);text-transform:uppercase;letter-spacing:.045rem;width:100%;margin-bottom:1rem;padding:0;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.75rem;font-weight:300;line-height:120%;display:block}
.tvg_collection_filters_sXMK-G_sort{flex-direction:column;gap:1rem;margin:0;padding:0;list-style:none;display:flex}
.tvg_collection_filters_sXMK-G_sort__option{align-items:center;display:flex}
.tvg_collection_filters_sXMK-G_sort__input{appearance:none;cursor:pointer;background:0 0;border:1px solid oklch(53.82% 0 0);border-radius:50%;flex-shrink:0;width:1rem;height:1rem;margin:0;transition:border-color .2s ease-in-out;position:relative}
.tvg_collection_filters_sXMK-G_sort__input:checked{border-color:oklch(34.85% 0 0)}
.tvg_collection_filters_sXMK-G_sort__input:checked:after{content:"";background:oklch(34.85% 0 0);border-radius:50%;position:absolute;inset:.125rem}
.tvg_collection_filters_sXMK-G_sort__input:focus-visible{outline-offset:2px;outline:2px solid oklch(34.85% 0 0)}
.tvg_collection_filters_sXMK-G_sort__label{cursor:pointer;color:oklch(34.85% 0 0);letter-spacing:-.01rem;margin-left:.5rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:1rem;font-weight:500;line-height:120%}
.tvg_collection_filters_sXMK-G_filter__container{flex-wrap:wrap;gap:.75rem 1rem;margin:0;padding:0;list-style:none;display:flex}
.tvg_collection_filters_sXMK-G_filter__container--product-type{gap:.75rem 1.25rem}
.tvg_collection_filters_sXMK-G_facet{margin:0}
.tvg_collection_filters_sXMK-G_facet__input{opacity:0;pointer-events:none;width:1px;height:1px;position:absolute}
.tvg_collection_filters_sXMK-G_facet__pill{cursor:pointer;color:oklch(34.85% 0 0);letter-spacing:-.01rem;background:0 0;border:1px solid oklch(94.31% 0 0);border-radius:624.938rem;align-items:center;padding:.5rem .75rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:1rem;font-weight:500;line-height:120%;transition:background-color .2s ease-in-out,border-color .2s ease-in-out;display:inline-flex}
.tvg_collection_filters_sXMK-G_facet__pill--product-type{border-radius:1rem;flex:calc(50% - .625rem);justify-content:center;gap:.75rem;height:3.25rem}
.tvg_collection_filters_sXMK-G_facet__pill--has-icon{min-width:9.25rem}
.tvg_collection_filters_sXMK-G_facet__icon{flex-shrink:0;justify-content:center;align-items:center;width:2.8125rem;height:100%;max-height:2.125rem;display:flex}
.tvg_collection_filters_sXMK-G_facet__icon :is(img,picture){object-fit:contain;width:auto;max-width:100%;height:auto;max-height:100%;display:block}
.tvg_collection_filters_sXMK-G_facet__label{min-width:0}
.tvg_collection_filters_sXMK-G_facet__input:checked+.tvg_collection_filters_sXMK-G_facet__pill{background-color:oklch(93.07% .0194 80.12);border-color:oklch(34.85% 0 0)}
.tvg_collection_filters_sXMK-G_facet__input:disabled+.tvg_collection_filters_sXMK-G_facet__pill{opacity:.4;pointer-events:none;cursor:not-allowed}
.tvg_collection_filters_sXMK-G_facet__input:focus-visible+.tvg_collection_filters_sXMK-G_facet__pill{outline-offset:2px;outline:2px solid oklch(34.85% 0 0)}
.tvg_collection_filters_sXMK-G_price{flex-flow:row;justify-content:flex-start;align-items:flex-end;gap:.875rem;display:flex}
.tvg_collection_filters_sXMK-G_price__label{margin-bottom:.5rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.75rem;font-weight:300;line-height:120%;display:block}
.tvg_collection_filters_sXMK-G_price__from input[type=number],.tvg_collection_filters_sXMK-G_price__to input[type=number]{border:1px solid oklch(53.82% 0 0);border-radius:0;min-width:6.875rem;padding:.75rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.75rem;font-weight:300;line-height:120%}
.tvg_collection_filters_sXMK-G_footer{background:oklch(99.59% .0093 99.98);border-top:1px solid oklch(34.85% 0 0);flex-direction:row;flex-shrink:0;align-items:center;gap:1.125rem;width:100%;margin-top:auto;padding:1.5rem 1rem;display:flex}
.tvg_collection_filters_sXMK-G_submit{appearance:none;color:oklch(99.59% .0093 99.98);text-transform:uppercase;letter-spacing:.0525rem;cursor:pointer;background:oklch(21.71% .0232 69.07);border:1px solid oklch(21.71% .0232 69.07);border-radius:4rem;flex:0 0 calc(50% - .5625rem);justify-content:center;align-items:center;gap:.5rem;min-height:2.375rem;margin:0 0 0 auto;padding:.25rem 1rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.875rem;font-weight:400;line-height:120%;text-decoration:none;transition:color .3s ease-in-out,background-color .3s ease-in-out,border-color .3s ease-in-out;display:inline-flex}
.tvg_collection_filters_sXMK-G_submit:disabled{opacity:.4;cursor:not-allowed}
.tvg_collection_filters_sXMK-G_reset{appearance:none;color:oklch(34.85% 0 0);text-transform:uppercase;letter-spacing:.0525rem;cursor:pointer;background:oklch(93.07% .0194 80.12);border:1px solid oklch(93.07% .0194 80.12);border-radius:4rem;flex:1 1 0;justify-content:center;align-items:center;gap:.5rem;min-height:2.375rem;margin:0;padding:.25rem 1rem;font-family:proxima-nova,-apple-system,blinkmacsystemfont,Segoe UI,roboto,oxygen-sans,ubuntu,cantarell,Helvetica Neue,sans-serif;font-size:.875rem;font-weight:400;line-height:120%;text-decoration:none;transition:color .3s ease-in-out,background-color .3s ease-in-out,border-color .3s ease-in-out;display:inline-flex}
.tvg_collection_filters_sXMK-G_reset:disabled{opacity:.4;cursor:not-allowed}
@media print,screen and (width>=40em){.tvg_collection_filters_sXMK-G_submit{flex:none;min-width:9.5rem;margin:0}
.tvg_collection_filters_sXMK-G_reset{flex:none;min-width:9.5rem}
}
@media print,screen and (width>=64em){.tvg_collection_filters_sXMK-G_root{width:100%;max-width:31.875rem}
.tvg_collection_filters_sXMK-G_sort__input:hover,.tvg_collection_filters_sXMK-G_facet__pill:hover{border-color:oklch(34.85% 0 0)}
}
@media print,screen and (width<64em){.tvg_collection_filters_sXMK-G_root.is-mobile-left{left:0;right:auto;transform:translate(-100%)translateZ(0)}
.tvg_collection_filters_sXMK-G_root.is-expanded.is-mobile-left{transform:translate(0%)translateY(0%)translateZ(0)}
}
@media (hover:hover){.tvg_collection_filters_sXMK-G_submit:not(:disabled):hover{color:oklch(21.71% .0232 69.07);background-color:oklch(99.59% .0093 99.98)}
.tvg_collection_filters_sXMK-G_reset:not(:disabled):hover{border-color:oklch(34.85% 0 0)}
}
</style>

{% doc %}
  Combined "Filter & Sort" drawer powered by the Search & Discovery app.
  Renders the collection (or search) filters as pill toggles, plus an optional
  "Sort by" radio group, inside a single offcanvas panel.

  @param {object} [filters] - Filter list. Defaults to `collection.filters`.

  Pass `search.filters` for the search context.

  @param {boolean} [show_filters] - Render the filter groups. Default true.
  @param {boolean} [show_sort] - Render the "Sort by" radio group. Default false.
  @param {boolean} [sort_context] - When true, read sort state from `search`

  instead of `collection`.

  @param {boolean} [ajax] - When true, the drawer applies filter/sort changes

  in place via the Section Rendering API (see `collection-results-refresh.ts`) instead of navigating. Falls back to
  navigation when false.

  Examples:

  Collection (combined, live updates): {% render 'collection-filters', show_sort: true, ajax: true %}

  Search (filters only, navigation fallback): {% render 'collection-filters', filters: search.filters %}
{% enddoc %}

{% liquid
  assign filters = filters | default: collection.filters
  assign submit_label = 'collections.filter.apply' | t

  if show_filters == null
    assign show_filters = true
  endif

  assign sort_values = 'manual,created-descending,price-ascending,price-descending' | split: ','

  if sort_context
    assign current_sort_by = search.sort_by | default: search.default_sort_by
    assign default_sort_by = search.default_sort_by
  else
    assign current_sort_by = collection.sort_by | default: collection.default_sort_by
    assign default_sort_by = collection.default_sort_by
  endif

  # Anything outside our curated set collapses to "Featured" (manual).
  unless sort_values contains current_sort_by
    assign current_sort_by = 'manual'
  endunless

  unless sort_values contains default_sort_by
    assign default_sort_by = 'manual'
  endunless

  # Total applied filters, surfaced in the footer "Clear (NN)" label. The JS
  # keeps this in sync as pills toggle (see index.tsx `updateTriggerCount`).
  assign active_count = 0
  for filter in filters
    assign active_count = active_count | plus: filter.active_values.size
  endfor

  assign has_active_filters = false
  if active_count > 0
    assign has_active_filters = true
  endif

  assign has_non_default_sort = false
  if show_sort and current_sort_by != default_sort_by
    assign has_non_default_sort = true
  endif

  assign clear_count = active_count
  if active_count < 10
    assign clear_count = '0' | append: active_count
  endif
%}

<div class="tvg_collection_filters_sXMK-G_overlay js-offcanvas-bg"></div>

<aside
  class="js-offcanvas tvg_collection_filters_sXMK-G_root"
  data-drawer-names="filters"
  role="dialog"
  aria-modal="true"
  aria-labelledby="filters-title"
  tabindex="-1"
  inert
  aria-hidden="true"
  data-section-id="{{ section.id }}"
  data-section-name="collection_filters"
  data-tvg-track-section-name="collection_filters"
>
  <div class="tvg_collection_filters_sXMK-G_container js-filters-body">
    <div class="tvg_collection_filters_sXMK-G_header">
      <h2 id="filters-title" class="tvg_collection_filters_sXMK-G_title">
        {{ 'collections.filter.button' | t }}
      </h2>
      <button type="button" class="js-offcanvas-close tvg_collection_filters_sXMK-G_close">
        {% render 'icon-close' %}
        <span class="u-vhide">{{ 'accessibility.icons.close' | t }}</span>
      </button>
    </div>

    <form
      id="js-filters-form"
      class="tvg_collection_filters_sXMK-G_form"
      aria-label="{{ 'collections.general.controls' | t }}"
      data-collection-ajax="{% if ajax %}true{% else %}false{% endif %}"
      {% if show_sort %}
        data-default-sort-by="{{ default_sort_by }}"
      {% endif %}
    >
      {%- if show_sort -%}
        <fieldset class="tvg_collection_filters_sXMK-G_group">
          <legend class="tvg_collection_filters_sXMK-G_group__title">{{ 'collections.sorting.sort_by' | t }}</legend>
          <ul
            class="tvg_collection_filters_sXMK-G_sort"
            role="radiogroup"
            aria-label="{{ 'collections.sorting.sort_by' | t }}"
          >
            {%- for value in sort_values -%}
              {%- liquid
                # Literal keys (not built dynamically) so theme-check can verify them.
                case value
                  when 'manual'
                    assign sort_name = 'collections.sorting.manual' | t
                  when 'created-descending'
                    assign sort_name = 'collections.sorting.newest' | t
                  when 'price-ascending'
                    assign sort_name = 'collections.sorting.price_ascending' | t
                  when 'price-descending'
                    assign sort_name = 'collections.sorting.price_descending' | t
                endcase
                assign sort_id = value | append: '-' | append: forloop.index
              -%}
              <li class="tvg_collection_filters_sXMK-G_sort__option js-sort-option">
                <input
                  type="radio"
                  name="sort_by"
                  value="{{ value }}"
                  id="sort-{{ sort_id }}"
                  class="tvg_collection_filters_sXMK-G_sort__input js-collection-sort"
                  {% if current_sort_by == value %}
                    checked
                  {% endif %}
                >
                <label class="tvg_collection_filters_sXMK-G_sort__label" for="sort-{{ sort_id }}">
                  {{- sort_name -}}
                </label>
              </li>
            {%- endfor -%}
          </ul>
        </fieldset>
      {%- endif -%}

      {%- if show_filters -%}
        {%- for filter in filters -%}
          {%- liquid
            assign filter_label = filter.label | downcase | handle
            assign is_product_type_group = false

            if filter_label == 'product-type'
              assign is_product_type_group = true
            endif
          -%}
          <fieldset class="tvg_collection_filters_sXMK-G_group">
            <legend class="tvg_collection_filters_sXMK-G_group__title">{{ filter.label }}</legend>

            {%- case filter.type -%}
              {%- when 'boolean', 'list' -%}
                <ul
                  class="tvg_collection_filters_sXMK-G_filter__container{% if is_product_type_group %} tvg_collection_filters_sXMK-G_filter__container--product-type{% endif %}"
                >
                  {%- for filter_value in filter.values -%}
                    {%- liquid
                      assign facet_id = 'filter-' | append: filter_label | append: '-' | append: forloop.index
                      assign filter_value_handle = filter_value.label | handle
                      assign filter_value_param_handle = filter_value.value | handle
                      assign product_type_icon = null

                      if is_product_type_group
                        for product_type_entry in shop.metaobjects.filters_product_type.values
                          assign entry_handle = product_type_entry.system.handle
                          assign entry_name_handle = product_type_entry.name | handle

                          if entry_handle == filter_value_handle or entry_handle == filter_value_param_handle or entry_name_handle == filter_value_handle or entry_name_handle == filter_value_param_handle
                            assign product_type_icon = product_type_entry.icon.value | default: product_type_entry.icon
                            break
                          endif
                        endfor
                      endif
                    -%}
                    <li class="tvg_collection_filters_sXMK-G_facet js-facet">
                      <input
                        type="checkbox"
                        name="{{ filter_value.param_name }}"
                        value="{{ filter_value.value }}"
                        id="{{ facet_id }}"
                        class="tvg_collection_filters_sXMK-G_facet__input js-collection-filter"
                        {% if filter_value.active %}
                          checked
                        {% endif %}
                        {% if filter_value.count == 0 and filter_value.active == false %}
                          disabled
                        {% endif %}
                      >
                      <label
                        class="tvg_collection_filters_sXMK-G_facet__pill{% if is_product_type_group %} tvg_collection_filters_sXMK-G_facet__pill--product-type{% endif %}{% if product_type_icon != blank %} tvg_collection_filters_sXMK-G_facet__pill--has-icon{% endif %}"
                        for="{{ facet_id }}"
                      >
                        <span class="tvg_collection_filters_sXMK-G_facet__label">{{ filter_value.label }}</span>
                        {%- if product_type_icon != blank -%}
                          <span class="tvg_collection_filters_sXMK-G_facet__icon">
                            {%- render 'core-image',
                              image: product_type_icon,
                              image_alt_text: filter_value.label,
                              mode: 'contain',
                              object_fit: 'contain',
                              lazyload: true,
                              srcset_print_sizes: '30,60'
                            -%}
                          </span>
                        {%- endif -%}
                      </label>
                    </li>
                  {%- endfor -%}
                </ul>

              {%- when 'price_range' -%}
                <div class="tvg_collection_filters_sXMK-G_price">
                  <div class="tvg_collection_filters_sXMK-G_price__from">
                    <label class="tvg_collection_filters_sXMK-G_price__label" for="filter-{{ filter.min_value.param_name }}">
                      {{- 'collections.filter.price_from' | t -}}
                    </label>
                    <input
                      name="{{ filter.min_value.param_name }}"
                      id="filter-{{ filter.min_value.param_name }}"
                      {% if filter.min_value.value -%}
                        value="{{ filter.min_value.value | money_without_currency | replace: ',', '' }}"
                      {%- endif %}
                      type="number"
                      placeholder="{{ cart.currency.symbol }}0"
                      min="0"
                      max="{{ filter.range_max | money_without_currency | replace: ',', '' }}"
                    >
                  </div>
                  <div class="tvg_collection_filters_sXMK-G_price__to">
                    <label class="tvg_collection_filters_sXMK-G_price__label" for="filter-{{ filter.max_value.param_name }}">
                      {{- 'collections.filter.price_to' | t -}}
                    </label>
                    <input
                      name="{{ filter.max_value.param_name }}"
                      id="filter-{{ filter.max_value.param_name }}"
                      {% if filter.max_value.value -%}
                        value="{{ filter.max_value.value | money_without_currency | replace: ',', '' }}"
                      {%- endif %}
                      type="number"
                      placeholder="{{ cart.currency.symbol }}{{ filter.range_max | money_without_currency | replace: ',', '' }}"
                      min="0"
                      max="{{ filter.range_max | money_without_currency | replace: ',', '' }}"
                    >
                  </div>
                </div>
            {%- endcase -%}
          </fieldset>
        {%- endfor -%}
      {%- endif -%}
    </form>

    <div class="tvg_collection_filters_sXMK-G_footer">
      <button
        type="button"
        class="tvg_collection_filters_sXMK-G_reset js-reset-filters"
        {% unless has_active_filters %}
          disabled
        {% endunless %}
      >
        <span class="">{{ 'collections.filter.clear' | t }}</span>
        <span class=" js-filter-clear-count">({{ clear_count }})</span>
      </button>

      <button
        type="button"
        class="tvg_collection_filters_sXMK-G_submit js-submit-filters js-offcanvas-close"
        {% unless has_active_filters or has_non_default_sort %}
          disabled
        {% endunless %}
      >
        {{- submit_label -}}
      </button>
    </div>
  </div>
</aside>

</div>

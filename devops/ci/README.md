# CI scripts

The files in this directory are meant to be run directly in the CI environment,
outside of the Docker container.

The scripts should target Linux (Debian/Ubuntu) AMD64 architecture, but we should strive to make them as portable as possible.

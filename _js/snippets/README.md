# Portable Snippets

Run `bb scaffold:snippet` to create a new portable snippet.

Expected file structure:

```
_js/snippets/[SNIPPET NAME]
├── index.tsx (optional)
├── schema.json
├── snippet.liquid
└── styles.scss
```
